services:
  - type: web
    name: synccode-server
    runtime: node
    rootDir: apps/server
    buildCommand: pnpm install && pnpm build
    startCommand: node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production